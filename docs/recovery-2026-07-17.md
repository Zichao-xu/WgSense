# WgSense Recovery Record

## Incident

On 2026-07-17 the tracked working tree was reset to commit `4741172`, after
which `ProxyView.swift` was edited into a non-compiling state. Untracked files
survived the reset.

## Recovery Sources

- The complete repository was copied before recovery work began.
- Tracked source files were restored from WorkBuddy's synchronized file-history
  snapshot taken at 2026-07-16 23:38:45 local time, before the reset.
- Untracked source, tests, packaging files, localization, and widget files were
  copied from the preserved working tree.
- `project.yml` was made authoritative and `WgSense.xcodeproj` was regenerated
  with XcodeGen. It includes the app, widget extension, localization resources,
  and bundled daemon build phase.
- Go dependency metadata was regenerated with `go mod tidy`.

## Verification

- `go test ./...`
- `go vet ./...`
- `go test -race ./internal/policy ./internal/proxy ./internal/transfer ./internal/tunnel`
- Xcode Debug build with signing disabled
- Xcode Release build with signing disabled
- Property-list and shell-script syntax checks
- `git diff --check`

No running daemon, tunnel, route, DNS, or other live network state was changed
during recovery.
